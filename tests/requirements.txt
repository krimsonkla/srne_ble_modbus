# BLE Testing Requirements
# Install with: pip install -r requirements.txt

# BLE library (cross-platform)
bleak>=0.21.0
bleak-retry-connector>=3.4.0

# dbus-fast: pytest-homeassistant-custom-component patches platform.system()
# to return "Linux" for bluetooth adapter simulation, which forces
# bleak-retry-connector's Linux-only imports at load time (dbus_fast is
# imported unconditionally when IS_LINUX=True). Required for BLE-adjacent
# tests to import on macOS.
dbus-fast

# Home Assistant (for testing components)
homeassistant>=2024.11.0

# Home Assistant dependencies
pyserial>=3.5

# Testing framework
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-timeout>=2.1.0
pytest-homeassistant-custom-component>=0.13.0
pytest-cov>=4.1.0

# Mocking
pytest-mock>=3.11.1
